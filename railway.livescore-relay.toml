# Config para livescore-relay — imagen Docker mínima (sin Next.js build).
# Deploy: powershell -File scripts/deploy-livescore-relay.ps1

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.livescore-relay"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
